# AI-assisted Clinical Forms - Environment Configuration

# =============================================================================
# Backend API Configuration
# =============================================================================

# Backend API service URL
# Default: https://ibm-datathon-api-gateway.onrender.com
BACKEND_API_URL=https://ibm-datathon-api-gateway.onrender.com

# HTTP client settings
HTTP_TIMEOUT=30
HTTP_RETRY_ATTEMPTS=3

# =============================================================================
# Application Settings
# =============================================================================

# Default doctor for demo purposes
DEFAULT_DOCTOR_ID=D001
DEFAULT_DOCTOR_NAME="Dr. Demo"

# AI/ASR configuration
ASR_MODEL=whisper-small
AI_AUTOFILL_ENABLED=true

# Audio configuration
AUDIO_SAMPLE_RATE=16000
AUDIO_CHUNK_SIZE=1024

# UI configuration
PATIENTS_PER_PAGE=12
TRANSCRIPT_MAX_LINES=50

# =============================================================================
# Development Settings
# =============================================================================

# Enable debug mode for additional logging
DEBUG=false

# Use mock API instead of real backend (for development/testing)
# Set to 'true' to use in-memory mock data
# Set to 'false' to use real backend API
MOCK_API=false

# =============================================================================
# Notes
# =============================================================================

# 1. Authentication: The backend API does not require authentication
# 2. CORS: Backend is configured to allow requests from any origin
# 3. Form Submission: Backend expects complete form submission with all 
#    symptoms and medications in a single request
# 4. Form Retrieval: Backend does not support retrieving forms after submission
# 5. Field Mappings:
#    - Frontend 'name' → Backend 'full_name'
#    - Frontend 'id' → Backend 'patient_id'/'doctor_id'
#    - Frontend 'date_of_birth' → Backend 'dob'
#    - Symptom/medication fields are converted from strings to integers
